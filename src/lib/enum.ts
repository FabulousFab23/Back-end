export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum GenderEnum {
  MALE = 'm',
  FEMALE = 'f',
  OTHER = 'other'
}

export enum FriendsStatusEnum {
  ACCEPTED = 'accepted',
  PENDING = 'pending',
  CANCELED = 'canceled',
}
